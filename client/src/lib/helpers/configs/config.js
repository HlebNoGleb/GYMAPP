let config = {
    useServer: false,
    useServer2: true, // временно, тк нужно получать данные с localstorage
    saveServerData: true,
    localStorageKeyPrefix: "GymApp-",
    apiUrl: "https://localhost:44361",
}

export default config;